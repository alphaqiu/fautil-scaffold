# 应用示例环境变量文件
# 将此文件复制为.env并根据需要修改
# 可以放在项目根目录、用户主目录下的.fautil目录或其他支持的位置

# 应用基本配置
FAUTIL_APP_TITLE=环境变量配置示例应用
FAUTIL_APP_DEBUG=true
FAUTIL_APP_PORT=8000
FAUTIL_APP_JWT_SECRET=your-jwt-secret-for-dev-environment

# 同样支持使用双下划线设置嵌套配置
FAUTIL_APP__DESCRIPTION=通过环境变量配置的应用
FAUTIL_APP__CORS_ORIGINS=http://localhost:3000,https://example.com

# 数据库配置
FAUTIL_DB__URL=postgresql://username:password@localhost:5432/dbname
FAUTIL_DB__ECHO=false
FAUTIL_DB__POOL_SIZE=5

# Redis配置
FAUTIL_REDIS__URL=redis://localhost:6379
FAUTIL_REDIS__DB=0

# 日志配置
FAUTIL_LOG_LEVEL=DEBUG
FAUTIL_LOG_FILE_PATH=/tmp/app.log

# Minio配置
FAUTIL_MINIO__ENDPOINT=localhost:9000
FAUTIL_MINIO__ACCESS_KEY=minioadmin
FAUTIL_MINIO__SECRET_KEY=minioadmin

# Kafka配置
FAUTIL_KAFKA__BOOTSTRAP_SERVERS=localhost:9092
FAUTIL_KAFKA__GROUP_ID=app-group
